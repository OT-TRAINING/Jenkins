pipeline {
    agent any
    tools {
        maven'maven-3.9.0'
        jdk 'jdk'
    }
    stages {
        stage('Build') {
            steps {
                echo 'Building with Maven'
                sh 'mvn --version'
               
            }
        }
    }
}
